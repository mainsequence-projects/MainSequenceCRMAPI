interface:
  display_name: "CRM FastAPI contracts"
  short_description: "CRM FastAPI contracts with pinned Main Sequence contracts"
  default_prompt: "Use $build-mainsequence-crm-api to implement the requested CRM work following the pinned handoff and existing platform boundaries."
