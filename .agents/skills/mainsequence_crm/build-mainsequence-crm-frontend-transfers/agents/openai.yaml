interface:
  display_name: "CRM migration frontend"
  short_description: "CRM migration frontend with pinned Main Sequence contracts"
  default_prompt: "Use $build-mainsequence-crm-frontend-transfers to implement the requested CRM work following the pinned handoff and existing platform boundaries."
