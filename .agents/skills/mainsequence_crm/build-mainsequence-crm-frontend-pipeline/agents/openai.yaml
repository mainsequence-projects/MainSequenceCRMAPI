interface:
  display_name: "CRM pipeline and merge UI"
  short_description: "CRM pipeline and merge UI with pinned Main Sequence contracts"
  default_prompt: "Use $build-mainsequence-crm-frontend-pipeline to implement the requested CRM work following the pinned handoff and existing platform boundaries."
